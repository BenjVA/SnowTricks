{% extends 'base.html.twig' %}

{% block body %}
    {{ form_errors(form) }}


    {{ form_start(form) }}
        {{ form_row(form.name) }}
        {{ form_row(form.description) }}
        {{ form_row(form.group_tricks) }}
        {{ form_row(form.images) }}

        {{ form_row(form.submit) }}
    {{ form_end(form) }}
{% endblock %}
