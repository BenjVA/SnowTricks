{% extends 'base.html.twig' %}

{% block title %}Détails de {{ trick.name }}{% endblock %}

{% block body %}
    <main class="container">
        <section class="row">
            <div class="col-12">
                <h1>Détails de {{ trick.name }}</h1>
            </div>
            <div>
                Images
            </div>
            <div>
                Catégorie: {{ trick.groupTricks }}
                <p>{{ trick.description }}</p>
            </div>
        </section>
    </main>
{% endblock %}
